# Regenerate Hub Pages After JSON Fixes

Once all JSON files are fixed, regenerate category hub pages:

```bash
cd bjjgraph && python scripts/generate_category_hub_pages.py
```

This will update all descriptions from fallback to proper content from JSON files.
